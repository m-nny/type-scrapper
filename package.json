{
  "name": "@m-nny/type-scrapper",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "build:clean": "rimraf services/**/dist && yarn build",
    "---workspaces---": "",
    "common": "yarn workspace @m-nny/common",
    "igram": "yarn workspace @m-nny/igram",
    "queue": "yarn workspace @m-nny/queue",
    "server": "yarn workspace @m-nny/server",
    "worker": "yarn workspace @m-nny/worker"
  },
  "workspaces": [
    "services/**"
  ],
  "devDependencies": {
    "rimraf": "^3.0.2",
    "ts-node": "^9.1.1",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.2.4"
  }
}
